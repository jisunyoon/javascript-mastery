# Week 3: 클로저 & 고차함수 🎭

클로저와 고차함수를 활용한 강력한 패턴을 익힙니다.

## 📚 학습 목표

- 클로저로 private 변수 만들기
- 고차함수 활용 (함수를 반환하는 함수)
- 메모이제이션 패턴
- 커링과 부분 적용

## 📝 문제 목록

1. **createCounter**: 클로저 기반 카운터
2. **memoize**: 메모이제이션 구현
3. **curry**: 커링 구현
4. **partial**: 부분 적용 구현
5. **once**: 한 번만 실행되는 함수
6. **createQueue**: 큐 자료구조
7. **createTimer**: 타이머 관리

## 💡 핵심 개념

```javascript
// 클로저: 외부 변수 기억
function outer() {
  let count = 0;  // private 변수
  return function inner() {
    count++;
    return count;
  };
}

// 고차함수: 함수를 반환
const multiply = (x) => (y) => x * y;
const double = multiply(2);
double(5); // 10
```
